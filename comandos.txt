1) git init --- Por proyecto

2) git add . --- Cada vez que halla un cambio

   Una sola vez -----------> git cnfig --global user.name "FERNANDO-GONZALEZ"
   Una sola vez -----------> git config --global user.email "fcprimaveral@gmail.com"
   Una sola vez -----------> git config --list

3) git commit -m "Comentario"  ---Se comenta el commit

4) git remote add origin https://github.com/FERNANDO-GONZALEZ/...

5) git push origen master

            git branch  --- identifica la rama actual

            git branch -M main  --- Cambia el nombre de la rama master

            git branch dev  --- Crea la rama dev

            git checkout dev --- Se pasa a la rama dev

            git branch -d qa  --- Eliminar rama qa
            
            git push --delete Rama --- Eliminar Rama

            git pull origin Rama --- Actualiza la rama con nuevos archivos compartidos por otros usuarios

            git clone url  --- Clona el repositorio de la url en mensión
            git clone --branch Rama url   ---igual
            git clone -b Rama url         ---igual

           git clone --branch Fernando https://github.com/FERNANDO-GONZALEZ/M-dulo-Uno---Git.git ---Personal
           git clone https://github.com/usuario--externo/Nombre--repositorio-Git.git --- De usuario externo

/* He logrado clonar el repositorio del profe David*/







